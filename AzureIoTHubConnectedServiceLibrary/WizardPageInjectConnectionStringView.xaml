<UserControl x:Class="AzureIoTHubConnectedService.WizardPageInjectConnectionStringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AzureIoTHubConnectedService"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="SummaryView">
    <Grid Name="MainGrid" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Device Primary Connection String" Grid.ColumnSpan="2" Grid.Row="0" Margin="5"  VerticalAlignment="Bottom" />

        <TextBox Text="{Binding DevicePrimaryConnectionString, Mode=OneWay}" Grid.ColumnSpan="2" Grid.Row="1" IsReadOnly="True" Margin="5" MinLines="2" TextWrapping="Wrap"/>

        <Button Grid.Row="2" Grid.Column="1" Margin="5" Command="{Binding Path=ExecuteCmd}" CommandParameter="ProvisionDevice">Provision Device</Button>
    </Grid>
</UserControl>
